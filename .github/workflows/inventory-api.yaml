name: ci-inventory-api

on:
    workflow_dispatch:
    pull_request:
        branches:
            - main
    push:
        branches:
            - feature/github-actions-ci

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repo
              uses: actions/checkout@v4

            - name: Confirm workflow is running
              run: echo "GitHub Actions CI test is working"