apiVersion: v1
kind: Service
metadata:
  name: inventory-api
  namespace: reliability-lab
spec:
  type: LoadBalancer
  selector:
    app: inventory-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080