apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-api
  #namespace: reliability-lab
  labels:
    app: inventory-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-api
  template:
    metadata:
      labels:
        app: inventory-api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /metrics
        prometheus.io/port: "8080"
    spec:
      nodeSelector:
        agentpool: app
      containers:
        - name: inventory-api
          image: devacr674.azurecr.io/inventory-api:v1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: OTEL_SERVICE_NAME
              value: inventory-api
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector.observability:4317
            - name: DEPLOYMENT_ENVIRONMENT
              value: dev
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-api
  #namespace: reliability-lab
spec:
  type: LoadBalancer
  selector:
    app: inventory-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
